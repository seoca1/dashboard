{
  "name": "game-dashboard",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Unified Projects Hub — Game/ Dashboard for Wet Run + Typing Language",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:data": "node scripts/aggregate-stats.mjs",
    "verify:data": "node scripts/verify-data.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}